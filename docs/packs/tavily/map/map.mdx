---
title: "map"
description: "Discover URLs on a site without extracting content. Use before crawl to understand site structure."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.tavily.com/map"}</code></div>

```python tavily_map.py
from charter.packs import tavily

result = await tavily.map.ainvoke({"url": ...})
```

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `url` <span className="required" title="required">*</span> | `string` | Root URL to begin mapping. |
| `instructions` | `string` | Natural language instructions for mapping. Doubles cost to 2 credits per 10 pages when set. |
| `maxDepth` | `integer` | Maximum mapping depth. The server applies 1 when this is absent. <span className="field-constraints">≥ 1; ≤ 5</span> |
| `maxBreadth` | `integer` | Maximum links per level. The server applies 20 when this is absent. <span className="field-constraints">≥ 1; ≤ 500</span> |
| `limit` | `integer` | Total links processed before stopping. The server applies 50 when this is absent. <span className="field-constraints">≥ 1</span> |
| `selectPaths` | `string`[] | Regex path patterns to include. |
| `selectDomains` | `string`[] | Regex domain or subdomain patterns to include. |
| `excludePaths` | `string`[] | Regex path patterns to exclude. |
| `excludeDomains` | `string`[] | Regex domain or subdomain patterns to exclude. |
| `allowExternal` | `boolean` | Include external domain links in results. The server applies true when this is absent. |
| `timeout` | `number` | Maximum wait in seconds. The server applies 150 when this is absent. <span className="field-constraints">≥ 10.0; ≤ 150.0</span> |
| `includeUsage` | `boolean` | Include credit usage in the response. |

</div>
