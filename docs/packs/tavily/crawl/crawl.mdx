---
title: "crawl"
description: "Graph-based website traversal with built-in extraction. Use when many pages on a site need to be read."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.tavily.com/crawl"}</code></div>

```python tavily_crawl.py
from charter.packs import tavily

result = await tavily.crawl.ainvoke({"url": ...})
```

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `url` <span className="required" title="required">*</span> | `string` | Root URL to begin the crawl. |
| `instructions` | `string` | Natural language instructions for the crawler. Doubles cost to 2 credits per 10 pages when set. |
| `chunksPerSource` | `integer` | Maximum chunks in each page's `raw_content`. The server applies 3 when this is absent. Available only when `instructions` is provided. <span className="field-constraints">≥ 1; ≤ 5</span> |
| `maxDepth` | `integer` | Maximum crawl depth from the base URL. The server applies 1 when this is absent. <span className="field-constraints">≥ 1; ≤ 5</span> |
| `maxBreadth` | `integer` | Maximum links per tree level. The server applies 20 when this is absent. <span className="field-constraints">≥ 1; ≤ 500</span> |
| `limit` | `integer` | Total links processed before stopping. The server applies 50 when this is absent. <span className="field-constraints">≥ 1</span> |
| `selectPaths` | `string`[] | Regex path patterns to include (for example `/docs/.*`). |
| `selectDomains` | `string`[] | Regex domain or subdomain patterns to include. |
| `excludePaths` | `string`[] | Regex path patterns to exclude. |
| `excludeDomains` | `string`[] | Regex domain or subdomain patterns to exclude. |
| `allowExternal` | `boolean` | Include external domain links in results. The server applies true when this is absent. |
| `includeImages` | `boolean` | Include images in crawl results. |
| `extractDepth` | [ExtractDepth](/packs/tavily/search/objects#extractdepth) | Page extraction depth. The server applies `basic` when this is absent. |
| `format` | [ContentFormat](/packs/tavily/search/objects#contentformat) | Extracted content format. The server applies `markdown` when this is absent. |
| `includeFavicon` | `boolean` | Include favicon URL per result. |
| `timeout` | `number` | Maximum wait in seconds. The server applies 150 when this is absent. <span className="field-constraints">≥ 10.0; ≤ 150.0</span> |
| `includeUsage` | `boolean` | Include credit usage in the response. |

</div>
