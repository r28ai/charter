---
title: "extract"
description: "Extract clean markdown or text from one or more known URLs. Use after search when the URL is already selected."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.tavily.com/extract"}</code></div>

```python tavily_extract.py
from charter.packs import tavily

result = await tavily.extract.ainvoke({"urls": ...})
```

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `urls` <span className="required" title="required">*</span> | `string` \| `string`[] | Single URL or list of URLs to extract (max 20). |
| `query` | `string` | User intent for reranking chunks. When provided, chunks are reranked and joined in `raw_content`. |
| `chunksPerSource` | `integer` | Maximum chunks per source in `raw_content`. The server applies 3 when this is absent. Available only when `query` is provided. <span className="field-constraints">≥ 1; ≤ 5</span> |
| `extractDepth` | [ExtractDepth](/packs/tavily/search/objects#extractdepth) | Extraction depth. The server applies `basic` when this is absent. `basic` costs 1 credit per 5 successful extractions; `advanced` costs 2 per 5. |
| `includeImages` | `boolean` | Include image URL list per result. |
| `includeFavicon` | `boolean` | Include favicon URL per result. |
| `format` | [ContentFormat](/packs/tavily/search/objects#contentformat) | Output format for extracted content. The server applies `markdown` when this is absent. |
| `timeout` | `number` | Maximum wait in seconds. The server applies 10 for `basic` and 30 for `advanced` when this is absent. <span className="field-constraints">≥ 1.0; ≤ 60.0</span> |
| `includeUsage` | `boolean` | Include credit usage in the response. |

</div>
