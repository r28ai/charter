---
title: "project_statuses_list"
description: "List the statuses a project can be in, with the UUIDs `project_create` and `project_update` need. Each has a `type`: 'backlog', 'planned', 'started', 'paused', 'completed' or 'canceled'."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.linear.app/graphql"}</code></div>

```python linear_project_statuses_list.py
from charter.packs import linear

result = await linear.project_statuses_list.ainvoke({})
```

<Note>
  Posts a GraphQL document to `POST /graphql`, as every tool in this pack does. Pages by cursor: `pageInfo.endCursor` is sent back as `variables.after`.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` | [PageVariables](/packs/linear/projects/objects#pagevariables) | Paging. |

</div>
