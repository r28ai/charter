---
title: "webhook_create"
description: "Create a webhook. Scope it to one team with `team_id` or to every public team with `all_public_teams`, and name the resource types it should fire for, such as 'Issue' or 'Comment'."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.linear.app/graphql"}</code></div>

```python linear_webhook_create.py
from charter.packs import linear

result = await linear.webhook_create.ainvoke({"variables": ...})
```

<Note>
  Posts a GraphQL document to `POST /graphql`, as every tool in this pack does.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` <span className="required" title="required">*</span> | [WebhookCreateVariables](/packs/linear/webhooks/objects#webhookcreatevariables) | The webhook to create. |

</div>
