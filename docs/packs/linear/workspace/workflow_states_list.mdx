---
title: "workflow_states_list"
description: "List workflow states — the statuses an issue can move between. Each has a `type` ('triage', 'backlog', 'unstarted', 'started', 'completed', 'canceled') and a UUID. States are per-team, so filter by team to get the right ones."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.linear.app/graphql"}</code></div>

```python linear_workflow_states_list.py
from charter.packs import linear

result = await linear.workflow_states_list.ainvoke({})
```

<Note>
  Posts a GraphQL document to `POST /graphql`, as every tool in this pack does. Pages by cursor: `pageInfo.endCursor` is sent back as `variables.after`.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` | [WorkflowStatesListVariables](/packs/linear/workspace/objects#workflowstateslistvariables) | Paging and filtering. |

</div>
