---
title: "customers_list"
description: "List customers — the companies whose requests Linear tracks against issues."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.linear.app/graphql"}</code></div>

```python linear_customers_list.py
from charter.packs import linear

result = await linear.customers_list.ainvoke({})
```

<Note>
  Posts a GraphQL document to `POST /graphql`, as every tool in this pack does. Pages by cursor: `pageInfo.endCursor` is sent back as `variables.after`.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` | [CustomersListVariables](/packs/linear/customers/objects#customerslistvariables) | Paging and filtering. |

</div>
