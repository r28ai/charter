---
title: "attachments_for_url"
description: "Find which issues an external URL is attached to. This is the tool for 'is this pull request already linked to an issue?'."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.linear.app/graphql"}</code></div>

```python linear_attachments_for_url.py
from charter.packs import linear

result = await linear.attachments_for_url.ainvoke({"variables": ...})
```

<Note>
  Posts a GraphQL document to `POST /graphql`, as every tool in this pack does. Pages by cursor: `pageInfo.endCursor` is sent back as `variables.after`.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` <span className="required" title="required">*</span> | [AttachmentsForUrlVariables](/packs/linear/attachments/objects#attachmentsforurlvariables) | The URL to look up. |

</div>
