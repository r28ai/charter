---
title: "attachment_create"
description: "Link an issue to something outside Linear. Linear deduplicates by URL: creating an attachment whose URL is already on the issue updates that one rather than adding a second."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.linear.app/graphql"}</code></div>

```python linear_attachment_create.py
from charter.packs import linear

result = await linear.attachment_create.ainvoke({"variables": ...})
```

<Note>
  Posts a GraphQL document to `POST /graphql`, as every tool in this pack does.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` <span className="required" title="required">*</span> | [AttachmentCreateVariables](/packs/linear/attachments/objects#attachmentcreatevariables) | The attachment to create. |

</div>
