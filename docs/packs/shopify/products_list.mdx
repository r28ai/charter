---
title: "products_list"
description: "List products. Narrow with a Shopify search string in `query`, e.g. 'status:active vendor:Acme' or 'inventory_total:<5'."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"/admin/api/2026-07/graphql.json"}</code></div>

```python shopify_products_list.py
from charter.packs import shopify

result = await shopify.products_list.ainvoke({})
```

<Note>
  Posts a GraphQL document to `POST /admin/api/2026-07/graphql.json`, as every tool in this pack does. Pages by cursor: `pageInfo.endCursor` is sent back as `variables.after`.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` | [ProductsListVariables](/packs/shopify/objects#productslistvariables) | Paging and filtering. |

</div>
