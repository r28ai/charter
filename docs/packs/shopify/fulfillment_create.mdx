---
title: "fulfillment_create"
description: "Fulfil one or more fulfillment orders, marking the goods as shipped. Get the ids from `order_fulfillment_orders` first. Omit the line items to fulfil a fulfillment order whole. Every one named must belong to the same order and the same location, so an order shipping from two places needs two calls."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"/admin/api/2026-07/graphql.json"}</code></div>

```python shopify_fulfillment_create.py
from charter.packs import shopify

result = await shopify.fulfillment_create.ainvoke({"variables": ...})
```

<Note>
  Posts a GraphQL document to `POST /admin/api/2026-07/graphql.json`, as every tool in this pack does.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `variables` <span className="required" title="required">*</span> | [FulfillmentCreateVariables](/packs/shopify/objects#fulfillmentcreatevariables) | The fulfilment to create. |

</div>
