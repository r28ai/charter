---
title: "balance_transactions_list"
description: "List every movement across the Stripe balance: charges, refunds, fees and payouts. For accounting, `reporting_category` on each result groups them better than `type` does."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://api.stripe.com/v1/balance_transactions"}</code></div>

```python stripe_balance_transactions_list.py
from charter.packs import stripe

result = await stripe.balance_transactions_list.ainvoke({})
```

<Note>
  Pages by cursor: `data[-1].id` is sent back as `starting_after`.
</Note>

## Query parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `limit` | `integer` | A limit on the number of objects to be returned, between 1 and 100. Defaults to 10. <span className="field-constraints">≥ 1; ≤ 100; defaults to `10`</span> |
| `startingAfter` | `string` | A cursor for use in pagination: an object ID that defines your place in the list. To get the next page, pass the id of the last object in the current page. |
| `endingBefore` | `string` | A cursor for use in pagination: an object ID that defines your place in the list. Returns the page before the named object. Mutually exclusive with starting_after. |
| `type` | [BalanceTransactionType](/packs/stripe/connect/objects#balancetransactiontype) | Only return transactions of this type. |
| `payout` | `string` | Only return transactions paid out in this payout. Automatic payouts only. |
| `source` | `string` | Only return transactions for this object ID. |
| `currency` | `string` | Three-letter lowercase ISO currency code. |
| `created` | [CreatedFilter](/packs/stripe/payments/objects#createdfilter) | Only return records created in this window. |

</div>
