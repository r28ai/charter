---
title: "git_blobs_get"
description: "Read a blob by SHA, decoded to text. Says so plainly instead when the blob is binary or over GitHub's 1MB ceiling for this endpoint."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://api.github.com/repos/{owner}/{repo}/git/blobs/{fileSha}"}</code></div>

```python github_git_blobs_get.py
from charter.packs import github

result = await github.git_blobs_get.ainvoke({"owner": ..., "repo": ..., "fileSha": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `fileSha` <span className="required" title="required">*</span> | `string` | The SHA of the blob, as it appears in a tree entry. |

</div>
