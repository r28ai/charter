---
title: "git_tags_create"
description: "Create an annotated tag object. This does not create the tag: follow it with `git_refs_create` pointing `refs/tags/NAME` at the SHA returned here. A lightweight tag needs only that second call."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.github.com/repos/{owner}/{repo}/git/tags"}</code></div>

```python github_git_tags_create.py
from charter.packs import github

result = await github.git_tags_create.ainvoke({"owner": ..., "repo": ..., "tag": ..., "message": ..., "object": ..., "type": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `tag` <span className="required" title="required">*</span> | `string` | The tag's name. This is typically a version (e.g., "v0.0.1"). |
| `message` <span className="required" title="required">*</span> | `string` | The tag message. |
| `object` <span className="required" title="required">*</span> | `string` | The SHA of the git object this is tagging. |
| `type` <span className="required" title="required">*</span> | [TagObjectType](/packs/github/git/objects#tagobjecttype) | The type of the object being tagged. Normally this is a `commit` but it can also be a `tree` or a `blob`. |
| `tagger` | [GitCommitAuthor](/packs/github/git/objects#gitcommitauthor) | An object with information about the individual creating the tag. |

</div>
