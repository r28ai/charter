---
title: "git_refs_delete"
description: "Delete a branch or tag, by reference. `heads/my-branch`, without the `refs/` prefix. GitHub refuses to delete the default branch."
tag: "DELETE"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="DELETE">DELETE</span><code>{"https://api.github.com/repos/{owner}/{repo}/git/refs/{ref}"}</code></div>

```python github_git_refs_delete.py
from charter.packs import github

result = await github.git_refs_delete.ainvoke({"owner": ..., "repo": ..., "ref": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `ref` <span className="required" title="required">*</span> | `string` | The Git reference to delete, formatted as `heads/BRANCH_NAME` or `tags/TAG_NAME`. Without the `refs/` prefix. |

</div>
