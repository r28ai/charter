---
title: "releases_get_by_tag"
description: "Get a release by its tag name, such as 'v1.4.0'. Usually more useful than `releases_get`, since a version string is something you already have and a release id is not."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://api.github.com/repos/{owner}/{repo}/releases/tags/{tag}"}</code></div>

```python github_releases_get_by_tag.py
from charter.packs import github

result = await github.releases_get_by_tag.ainvoke({"owner": ..., "repo": ..., "tag": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `tag` <span className="required" title="required">*</span> | `string` | The name of the tag, for example `v1.4.0`. |

</div>
