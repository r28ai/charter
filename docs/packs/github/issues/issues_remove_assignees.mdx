---
title: "issues_remove_assignees"
description: "Unassign people from an issue."
tag: "DELETE"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="DELETE">DELETE</span><code>{"https://api.github.com/repos/{owner}/{repo}/issues/{issueNumber}/assignees"}</code></div>

```python github_issues_remove_assignees.py
from charter.packs import github

result = await github.issues_remove_assignees.ainvoke({"owner": ..., "repo": ..., "issueNumber": ..., "assignees": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `issueNumber` <span className="required" title="required">*</span> | `integer` | The number that identifies the issue. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `assignees` <span className="required" title="required">*</span> | `string`[] | Usernames of the people to unassign from this issue. <span className="field-constraints">min 1 items</span> |

</div>
