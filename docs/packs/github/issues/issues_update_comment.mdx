---
title: "issues_update_comment"
description: "Edit an issue comment's text. This is how an agent corrects or extends something it already posted, rather than posting again."
tag: "PATCH"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="PATCH">PATCH</span><code>{"https://api.github.com/repos/{owner}/{repo}/issues/comments/{commentId}"}</code></div>

```python github_issues_update_comment.py
from charter.packs import github

result = await github.issues_update_comment.ainvoke({"owner": ..., "repo": ..., "commentId": ..., "body": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `commentId` <span className="required" title="required">*</span> | `integer` | The unique identifier of the comment — its `id`, not the issue number it sits on. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `body` <span className="required" title="required">*</span> | `string` | The contents of the comment. |

</div>
