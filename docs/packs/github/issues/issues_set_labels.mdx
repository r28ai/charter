---
title: "issues_set_labels"
description: "Replace an issue's labels with exactly this set, dropping any others. An empty list removes every label."
tag: "PUT"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="PUT">PUT</span><code>{"https://api.github.com/repos/{owner}/{repo}/issues/{issueNumber}/labels"}</code></div>

```python github_issues_set_labels.py
from charter.packs import github

result = await github.issues_set_labels.ainvoke({"owner": ..., "repo": ..., "issueNumber": ..., "labels": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `issueNumber` <span className="required" title="required">*</span> | `integer` | The number that identifies the issue. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `labels` <span className="required" title="required">*</span> | `string`[] | The names of the labels to set for the issue. These replace any existing labels. Pass an empty array to remove all labels. |

</div>
