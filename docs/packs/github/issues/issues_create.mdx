---
title: "issues_create"
description: "Open an issue. Only `title` is required. Labels must already exist on the repository, and assignees must have push access."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.github.com/repos/{owner}/{repo}/issues"}</code></div>

```python github_issues_create.py
from charter.packs import github

result = await github.issues_create.ainvoke({"owner": ..., "repo": ..., "body": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `body` <span className="required" title="required">*</span> | [IssueCreateBody](/packs/github/issues/objects#issuecreatebody) | The issue to create. |

</div>
