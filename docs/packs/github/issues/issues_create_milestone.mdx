---
title: "issues_create_milestone"
description: "Create a milestone. Only `title` is required."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.github.com/repos/{owner}/{repo}/milestones"}</code></div>

```python github_issues_create_milestone.py
from charter.packs import github

result = await github.issues_create_milestone.ainvoke({"owner": ..., "repo": ..., "title": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `title` <span className="required" title="required">*</span> | `string` | The title of the milestone. |
| `state` | [PullUpdateState](/packs/github/issues/objects#pullupdatestate) | The state of the milestone. GitHub uses `open` when this is absent. |
| `description` | `string` | A description of the milestone. |
| `dueOn` | `string` | The milestone due date. This is a timestamp in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ`. <span className="field-constraints">format `date-time`</span> |

</div>
