---
title: "issues_delete_label"
description: "Delete a label, removing it from every issue that carries it."
tag: "DELETE"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="DELETE">DELETE</span><code>{"https://api.github.com/repos/{owner}/{repo}/labels/{name}"}</code></div>

```python github_issues_delete_label.py
from charter.packs import github

result = await github.issues_delete_label.ainvoke({"owner": ..., "repo": ..., "name": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `name` <span className="required" title="required">*</span> | `string` | The name of the label to delete. |

</div>
