---
title: "pulls_delete_pending_review"
description: "Discard a review that was never submitted. Only a pending review can be deleted; a submitted one is dismissed instead."
tag: "DELETE"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="DELETE">DELETE</span><code>{"https://api.github.com/repos/{owner}/{repo}/pulls/{pullNumber}/reviews/{reviewId}"}</code></div>

```python github_pulls_delete_pending_review.py
from charter.packs import github

result = await github.pulls_delete_pending_review.ainvoke({"owner": ..., "repo": ..., "pullNumber": ..., "reviewId": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `pullNumber` <span className="required" title="required">*</span> | `integer` | The number that identifies the pull request. |
| `reviewId` <span className="required" title="required">*</span> | `integer` | The unique identifier of the review. |

</div>
