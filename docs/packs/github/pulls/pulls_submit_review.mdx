---
title: "pulls_submit_review"
description: "Submit a pending review as an approval, a request for changes, or a comment. A review stays invisible until this lands. Only needed for a review left pending — `pulls_create_review` submits in one step."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.github.com/repos/{owner}/{repo}/pulls/{pullNumber}/reviews/{reviewId}/events"}</code></div>

```python github_pulls_submit_review.py
from charter.packs import github

result = await github.pulls_submit_review.ainvoke({"owner": ..., "repo": ..., "pullNumber": ..., "reviewId": ..., "event": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `pullNumber` <span className="required" title="required">*</span> | `integer` | The number that identifies the pull request. |
| `reviewId` <span className="required" title="required">*</span> | `integer` | The unique identifier of the review. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `event` <span className="required" title="required">*</span> | [ReviewEvent](/packs/github/pulls/objects#reviewevent) | The review action to perform. GitHub: leaving this blank returns 422 and leaves the review PENDING. |
| `body` | `string` | The body text of the pull request review. |

</div>
