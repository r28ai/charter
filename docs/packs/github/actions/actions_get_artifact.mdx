---
title: "actions_get_artifact"
description: "Get one artifact's name, size, expiry and digest."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://api.github.com/repos/{owner}/{repo}/actions/artifacts/{artifactId}"}</code></div>

```python github_actions_get_artifact.py
from charter.packs import github

result = await github.actions_get_artifact.ainvoke({"owner": ..., "repo": ..., "artifactId": ...})
```

<Note>
  Requires `repo`, `read:user`.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `owner` <span className="required" title="required">*</span> | `string` | The account owner of the repository. The name is not case sensitive. |
| `repo` <span className="required" title="required">*</span> | `string` | The name of the repository without the `.git` extension. The name is not case sensitive. |
| `artifactId` <span className="required" title="required">*</span> | `integer` | The unique identifier of the artifact. |

</div>
