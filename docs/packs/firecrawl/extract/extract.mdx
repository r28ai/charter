---
title: "extract"
description: "Extract structured data from one or more URLs using an LLM. Poll results with extract_status."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.firecrawl.dev/v2/extract"}</code></div>

```python firecrawl_extract.py
from charter.packs import firecrawl

result = await firecrawl.extract.ainvoke({"urls": ...})
```

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `urls` <span className="required" title="required">*</span> | `string`[] | The URLs to extract data from. URLs should be in glob format. |
| `prompt` | `string` | Prompt to guide the extraction process. |
| `schema` | `object` | Schema to define the structure of the extracted data. Must conform to JSON Schema. |
| `enableWebSearch` | `boolean` | When true, the extraction will use web search to find additional data. |
| `ignoreSitemap` | `boolean` | When true, sitemap.xml files will be ignored during website scanning. |
| `includeSubdomains` | `boolean` | When true, subdomains of the provided URLs will also be scanned. The server applies true when this is absent. |
| `showSources` | `boolean` | When true, the sources used to extract the data will be included in the response as `sources`. |
| `scrapeOptions` | [ScrapeOptionsNested](/packs/firecrawl/search/objects#scrapeoptionsnested) | Options applied when scraping pages for extraction. |
| `ignoreInvalidURLs` | `boolean` | If invalid URLs are specified, they are ignored and returned in invalidURLs instead of failing the request. The server applies true when this is absent. |
| `threatProtection` | [ThreatProtectionOverride](/packs/firecrawl/search/objects#threatprotectionoverride) | Per-request threat protection override. Enterprise feature. |

</div>
