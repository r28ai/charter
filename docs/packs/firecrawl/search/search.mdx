---
title: "search"
description: "Search and optionally scrape web results. Use for current events, factual lookups, news, and any question that benefits from real-time web results. Supports web, image, and news sources. Include scrapeOptions with formats to get full page content."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.firecrawl.dev/v2/search"}</code></div>

```python firecrawl_search.py
from charter.packs import firecrawl

result = await firecrawl.search.ainvoke({"query": ...})
```

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `query` <span className="required" title="required">*</span> | `string` | The search query <span className="field-constraints">max length 500</span> |
| `limit` | `integer` | Maximum number of results to return per source type. The server applies 10 when this is absent. <span className="field-constraints">≥ 1; ≤ 100</span> |
| `sources` | `"web" \| "images" \| "news"` \| [SearchSource](/packs/firecrawl/search/objects#searchsource)[] | Sources to search. Strings or objects. The documented default is ['web']; objects add source-specific options such as tbs and location. |
| `categories` | `"developer" \| "research" \| "pdf"` \| [SearchCategory](/packs/firecrawl/search/objects#searchcategory)[] | Categories to filter results by. Strings or objects. The server applies no category filter when this is absent. |
| `includeDomains` | `string`[] | Restricts search results to these hostnames. Cannot be used with excludeDomains. |
| `excludeDomains` | `string`[] | Excludes search results from these hostnames. Cannot be used with includeDomains. |
| `tbs` | `string` | Time-based search parameter. Supports predefined time ranges (qdr:h, qdr:d, qdr:w, qdr:m, qdr:y), custom date ranges (cdr:1,cd_min:MM/DD/YYYY,cd_max:MM/DD/YYYY), and sort by date (sbd:1). |
| `location` | `string` | Location parameter for search results (e.g. San Francisco,California,United States). |
| `country` | `string` | ISO country code for geo-targeting. The server applies US when this is absent. |
| `safe` | `boolean` | When true, filters explicit content from search results (SafeSearch). |
| `timeout` | `integer` | Timeout in milliseconds. The server applies 60000 when this is absent. Milliseconds, not seconds: 30000 is thirty seconds, and 30 fails instantly. |
| `ignoreInvalidURLs` | `boolean` | Excludes URLs that are invalid for other Firecrawl endpoints. |
| `highlights` | `boolean` | Generate query-relevant highlights. The server applies true when this is absent. |
| `enterprise` | `"anon" \| "zdr"`[] | Enterprise Zero Data Retention options. ['zdr'] is end-to-end ZDR; ['anon'] is anonymized ZDR. |
| `scrapeOptions` | [ScrapeOptionsNested](/packs/firecrawl/search/objects#scrapeoptionsnested) | Options for scraping search results |
| `threatProtection` | [ThreatProtectionOverride](/packs/firecrawl/search/objects#threatprotectionoverride) | Per-request threat protection override. Enterprise feature. |

</div>
