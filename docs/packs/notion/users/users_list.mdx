---
title: "users_list"
description: "List the people and bots in the workspace. Guests are excluded, and there is no way to filter by name or email — page through and match locally. A personal access token cannot call this."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://api.notion.com/v1/users"}</code></div>

```python notion_users_list.py
from charter.packs import notion

result = await notion.users_list.ainvoke({})
```

<Note>
  Requires `Read user information`. Pages by cursor: `next_cursor` is sent back as `start_cursor`.
</Note>

## Query parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `startCursor` | `string` | A `next_cursor` value returned in a previous response. Returns the results immediately after this cursor; omit it to start from the beginning. |
| `pageSize` | `integer` | The number of items from the full list desired in the response. Maximum 100, and 100 is also what the server returns when this is absent. <span className="field-constraints">≥ 1; ≤ 100</span> |

</div>
