---
title: "search"
description: "Search the titles of pages and data sources shared with this integration. Titles only — it does not search page content — and it returns nothing that has not been shared. With no query it lists everything visible, which is how you find the IDs to start from."
tag: "POST"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="POST">POST</span><code>{"https://api.notion.com/v1/search"}</code></div>

```python notion_search.py
from charter.packs import notion

result = await notion.search.ainvoke({})
```

<Note>
  Requires `Read content`. Pages by cursor: `next_cursor` is sent back as `body.start_cursor`.
</Note>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `body` | [SearchBody](/packs/notion/search/objects#searchbody) | The query and its options. All optional. |

</div>
