---
title: "drafts_list"
description: "List the drafts in the user's mailbox."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://gmail.googleapis.com/gmail/v1/users/{userId}/drafts"}</code></div>

```python gmail_drafts_list.py
from charter.packs import gmail

result = await gmail.drafts_list.ainvoke({})
```

<Note>
  Requires `https://www.googleapis.com/auth/gmail.modify`. Pages by cursor: `nextPageToken` is sent back as `pageToken`. Costs 5 against the provider's own quota.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `userId` | `string` | The user's email address. The special value 'me' can be used to indicate the authenticated user. <span className="field-constraints">defaults to `"me"`</span> |

</div>

## Query parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `maxResults` | `integer` | Maximum number of drafts to return. Gmail returns 100 when this is absent. The maximum allowed value for this field is 500. <span className="field-constraints">≥ 1; ≤ 500</span> |
| `pageToken` | `string` | Page token to retrieve a specific page of results in the list. |
| `q` | `string` | Only return draft messages matching the specified query. Supports the same query format as the Gmail search box. For example, "from:someuser@example.com rfc822msgid:&lt;somemsgid@example.com&gt; is:unread". |
| `includeSpamTrash` | `boolean` | Include drafts from SPAM and TRASH in the results. |

</div>
