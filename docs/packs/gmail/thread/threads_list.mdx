---
title: "threads_list"
description: "List Gmail threads."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://gmail.googleapis.com/gmail/v1/users/{userId}/threads"}</code></div>

```python gmail_threads_list.py
from charter.packs import gmail

result = await gmail.threads_list.ainvoke({})
```

<Note>
  Requires `https://www.googleapis.com/auth/gmail.modify`. Pages by cursor: `nextPageToken` is sent back as `pageToken`. Costs 10 against the provider's own quota.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `userId` | `string` | The user's email address. The special value 'me' can be used to indicate the authenticated user. <span className="field-constraints">defaults to `"me"`</span> |

</div>

## Query parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `maxResults` | `integer` | Maximum number of threads to return (default 100, max 500). <span className="field-constraints">≥ 1; ≤ 500; defaults to `10`</span> |
| `pageToken` | `string` | Page token to retrieve a specific page of results in the list. |
| `q` | `string` | Only return threads matching this Gmail search query string. |
| `labelIds` | `string`[] | Return only threads with all of these label IDs. |
| `includeSpamTrash` | `boolean` | Include threads from SPAM and TRASH in the results. |

</div>
