---
title: "labels_update"
description: "Replace a label's name, visibility and colour."
tag: "PUT"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="PUT">PUT</span><code>{"https://gmail.googleapis.com/gmail/v1/users/{userId}/labels/{id}"}</code></div>

```python gmail_labels_update.py
from charter.packs import gmail

result = await gmail.labels_update.ainvoke({"id": ..., "body": ...})
```

<Note>
  Requires `https://www.googleapis.com/auth/gmail.modify`. Costs 5 against the provider's own quota.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `userId` | `string` | The user's email address. The special value 'me' can be used to indicate the authenticated user. <span className="field-constraints">defaults to `"me"`</span> |
| `id` <span className="required" title="required">*</span> | `string` | The ID of the label to update. |

</div>

## Body

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `body` <span className="required" title="required">*</span> | [Label](/packs/gmail/label/objects#label) | The label's replacement content. |

</div>
