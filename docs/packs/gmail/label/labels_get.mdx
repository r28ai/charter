---
title: "labels_get"
description: "Read one label, including its message and thread counts."
tag: "GET"
---

{/* generated by scripts/generate_pack_specs.py — do not edit by hand */}

<div className="tool-route"><span className="tool-row-method" data-method="GET">GET</span><code>{"https://gmail.googleapis.com/gmail/v1/users/{userId}/labels/{id}"}</code></div>

```python gmail_labels_get.py
from charter.packs import gmail

result = await gmail.labels_get.ainvoke({"id": ...})
```

<Note>
  Requires `https://www.googleapis.com/auth/gmail.modify`. Costs 1 against the provider's own quota.
</Note>

## Path parameters

<div className="reference-table">

| Field | Type | Description |
| --- | --- | --- |
| `userId` | `string` | The user's email address. The special value 'me' can be used to indicate the authenticated user. <span className="field-constraints">defaults to `"me"`</span> |
| `id` <span className="required" title="required">*</span> | `string` | The ID of the label to retrieve. |

</div>
